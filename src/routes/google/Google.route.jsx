/**
 * Google routes
 * =============
 *
 * Google routes are used to sign in and sign up users
 *
 */
const Google = () => {
  return (
    <div>
      <h1>Google</h1>
      <p>You reached the Google route</p>
    </div>
  );
};
export default Google;
